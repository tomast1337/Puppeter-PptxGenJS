name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build and test package
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          package-manager-cache: false

      - name: Install locked dependencies
        run: bun ci

      - name: Check formatting, lint, and complexity
        run: bun run check

      - name: Type-check
        run: bun run typecheck

      - name: Run unit tests
        run: bun test

      - name: Build package
        run: bun run build

      - name: Create installable tarball
        run: |
          mkdir -p artifacts
          bun pm pack --destination artifacts

      - name: Upload package artifact
        uses: actions/upload-artifact@v4
        with:
          name: package-${{ github.sha }}
          path: artifacts/*.tgz
          if-no-files-found: error
          retention-days: 14

  node-compatibility:
    name: Packed package on Node.js ${{ matrix.node }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        node: [22, 24]
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node }}
          package-manager-cache: false

      - name: Install locked dependencies
        run: bun ci

      - name: Build and test the packed package
        run: bun run test:package

  visual:
    name: Visual parity
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Install locked dependencies
        run: bun ci

      - name: Install visual comparison tools
        run: |
          sudo apt-get update
          sudo apt-get install --yes imagemagick libreoffice poppler-utils

      - name: Run visual comparison
        run: bun run test:visual
